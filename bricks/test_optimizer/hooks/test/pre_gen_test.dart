import 'package:test/test.dart';

void main() {
  group('Pre gen hook', () {
    test('Fails when target test dir does not exist', () {});
    test('Fails when target dir does not contain a pubspec.yaml', () {});
    test('Completes with test files list', () {});
  });
}
