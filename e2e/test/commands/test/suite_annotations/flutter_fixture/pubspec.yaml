name: suite_annotations_flutter
description: Fixture for testing library-level test annotations on Flutter.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
